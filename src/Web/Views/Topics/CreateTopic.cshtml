@using Web.Resources
@model TopicCreateModelWithImage

@{
    ViewData["Title"] = @Common.CreateTopic;
    Layout = "_Layout";
}

<p>@Common.CreateTopicNote</p>
<form method="post" enctype="multipart/form-data">>
    <div>
        <label>@Common.Title</label>
        <input type="text" class="form-control" placeholder="@Common.Title" asp-for="@Model.Title">
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>
    <div>
        <label>@Common.Content</label>
        <textarea class="form-control" placeholder="@Common.Content" style="height: 200px" asp-for="@Model.Content"></textarea>
        <span asp-validation-for="Content" class="text-danger"></span>
    </div>
    <div>
        <label>@Common.BackgroundImage</label>
        <input type="file" class="form-control" asp-for="@Model.Image" />
    </div>
    <div>
        <input type="submit" value="@Common.CreateTopic" class="btn btn-primary" />
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}